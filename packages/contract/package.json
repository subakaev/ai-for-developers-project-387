{
  "name": "@calls/contract",
  "version": "0.0.0",
  "private": true,
  "description": "TypeSpec API contract for the call booking service (source of truth)",
  "type": "module",
  "scripts": {
    "build": "tsp compile .",
    "watch": "tsp compile . --watch"
  },
  "devDependencies": {
    "@typespec/compiler": "^1.13.0",
    "@typespec/http": "^1.13.0",
    "@typespec/openapi3": "^1.13.0",
    "@typespec/rest": "^0.83.0"
  }
}
